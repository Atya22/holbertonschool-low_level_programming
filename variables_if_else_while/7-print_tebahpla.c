#include<stdio.h>
/**
*main - function that prints the lowwercase
*alphabet in reverse
*
*Return: always (0)
**/

int main(void)
{
char rev_letters
for (rev_letters = 'z'; rev_letters >= 'a'; rev_letters--)
{
putchar(rev_letters);
}
putchar('\n');
return (0);
}
